# ── Node ───────────────────────────────────────────────────────────────────
node_modules/
.pnpm-store/
*.tsbuildinfo

# ── Next ───────────────────────────────────────────────────────────────────
.next/
out/
next-env.d.ts

# ── Build output ───────────────────────────────────────────────────────────
dist/
build/

# ── Secrets. Never commit an env file with values in it. ───────────────────
.env
.env.*
!.env.example

# ── Local artefacts ────────────────────────────────────────────────────────
.turbo/
.screenshots/
packages/render/.render-output/
coverage/
*.log

# ── OS ─────────────────────────────────────────────────────────────────────
.DS_Store

# Playwright
/test-results/
/playwright-report/
/blob-report/
.render-output/

# Selector discovery and capture output. Regenerated by scripts/discover-selectors.ts
# and scripts/capture-flows.ts, and large.
.discovery/
apps/web/public/uploads/
# Cut product footage, written into the render bundle by the capture handler.
# Regenerated on every capture and large; §163.
packages/render/public/capture/
.vercel
.env*
supabase/.temp/

# Signed-in session for the review recording. Never commit.
.demo-auth.json

# Google OAuth client downloads. They contain a live client_secret and land in
# the repo root by default from the Cloud console. §192.
client_secret_*.apps.googleusercontent.com.json

# Downloaded production media for human review. Regenerable; not source.
media-review/
